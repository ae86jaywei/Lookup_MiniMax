name: 中望CAD动态块查寻插件 - 测试 构建

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  schedule:
    - cron: '0 2 * * *'

env:
  DOTNET_VERSION: '6.0.x'
  DOTNET_FRAMEWORK_VERSION: 'net48'
  PROJECT_NAME: 'ZWDynLookup'
  SOLUTION_NAME: 'ZWDynLookup.sln'
  TEST_PROJECT_NAME: 'ZWDynLookup.Tests'
  COVERAGE_THRESHOLD: 85

permissions:
  contents: read
  checks: write
  security-events: write

jobs:
  # 代码质量检查 - 简化版本
  code-quality:
    name: 代码质量检查
    runs-on: ubuntu-latest
    steps:
    - name: 检出代码
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: 设置 .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}

    - name: 还原NuGet包
      run: |
        dotnet restore ${{ env.SOLUTION_NAME }}
        dotnet restore ${{ env.TEST_PROJECT_NAME }}/${{ env.TEST_PROJECT_NAME }}.csproj

    - name: 检查SonarCloud配置
      shell: bash
      run: |
        echo "检查SonarCloud配置..."
        if [ -z "${{ secrets.SONAR_TOKEN }}" ]; then
          echo "警告: SONAR_TOKEN未设置，跳过SonarCloud分析"
          echo "SONARCLOUD_SKIP=true" >> $GITHUB_ENV
        else
          echo "SONAR_TOKEN已配置，继续执行SonarCloud分析"
          echo "SONARCLOUD_SKIP=false" >> $GITHUB_ENV
        fi

    - name: 安装SonarScanner
      if: env.SONARCLOUD_SKIP == 'false'
      run: dotnet tool install --global dotnet-sonarscanner

    - name: SonarCloud分析
      if: env.SONARCLOUD_SKIP == 'false'
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      shell: bash
      run: |
        echo "开始SonarCloud分析..."
        if [ -z "${{ secrets.SONAR_TOKEN }}" ]; then
          echo "错误: SONAR_TOKEN为空"
          exit 1
        fi
        
        dotnet sonarscanner begin \
          /key:"${{ env.PROJECT_NAME }}" \
              /organization:"ae86jaywei          /d:sonar.token="${{ secrets.SONAR_TOKEN }}" \
          /d:sonar.host.url="https://sonarcloud.io" \
          /d:sonar.cs.opencover.reportsPaths="**/coverage.opencover.xml" \
          /d:sonar.coverage.exclusions="**/*Test*/**,**/Tests/**"

    - name: 构建和测试（用于SonarCloud）
      if: env.SONARCLOUD_SKIP == 'false'
      run: |
        dotnet build ${{ env.SOLUTION_NAME }} --no-restore
        
        dotnet test ${{ env.TEST_PROJECT_NAME }}/${{ env.TEST_PROJECT_NAME }}.csproj \
          --no-build \
          --verbosity normal \
          --collect:"XPlat Code Coverage" \
          --results-directory TestResults \
          -- DataCollectionRunSettings.DataCollectors.DataCollector.Configuration.Format=opencover

    - name: 结束SonarCloud分析
      if: env.SONARCLOUD_SKIP == 'false'
      env:
        SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      shell: bash
      run: |
        echo "结束SonarCloud分析..."
        dotnet sonarscanner end /d:sonar.token="${{ secrets.SONAR_TOKEN }}"

    - name: 基本代码质量检查（无SonarCloud）
      if: env.SONARCLOUD_SKIP == 'true'
      shell: bash
      run: |
        echo "执行基本代码质量检查..."
        dotnet build ${{ env.SOLUTION_NAME }} --no-restore
        
        dotnet test ${{ env.TEST_PROJECT_NAME }}/${{ env.TEST_PROJECT_NAME }}.csproj \
          --no-build \
          --verbosity normal \
          --logger "console;verbosity=normal"
        
        echo "=== 代码质量检查 ==="
        echo "1. 构建: ✅ 成功"
        echo "2. 测试: ✅ 运行完成"
        echo "3. SonarCloud: ⚠️ 未配置"

  # Windows环境测试
  test-windows:
    name: Windows环境测试
    runs-on: windows-latest
    needs: code-quality
    
    strategy:
      matrix:
        configuration: [Debug, Release]
        framework: [net48]
        
    steps:
    - name: 检出代码
      uses: actions/checkout@v4

    - name: 设置 .NET Framework
      uses: microsoft/setup-msbuild@v1.1
      with:
        msbuild-version: '17.6'

    - name: 设置 .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}

    - name: 安装MSBuild
      run: |
        choco install msbuild --version 17.6.0

    - name: 还原NuGet包
      shell: pwsh
      run: |
        dotnet restore ${{ env.SOLUTION_NAME }}
        dotnet restore ${{ env.TEST_PROJECT_NAME }}/${{ env.TEST_PROJECT_NAME }}.csproj

    - name: 缓存NuGet包
      uses: actions/cache@v3
      with:
        path: ~/.nuget/packages
        key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj') }}
        restore-keys: |
          ${{ runner.os }}-nuget-

    - name: 构建项目 (${{ matrix.configuration }})
      shell: pwsh
      run: |
        Write-Host "构建项目 - ${{ matrix.configuration }} 模式" -ForegroundColor Green
        msbuild ${{ env.SOLUTION_NAME }} /p:Configuration=${{ matrix.configuration }} /p:Platform="Any CPU" /p:RestorePackages=true /v:minimal
        
        if (Test-Path "bin\${{ matrix.configuration }}\net48\ZWDynLookup.dll") {
          Write-Host "主项目构建成功" -ForegroundColor Green
        } else {
          Write-Host "主项目构建失败" -ForegroundColor Red
          exit 1
        }

    - name: 运行单元测试
      shell: pwsh
      run: |
        Write-Host "开始单元测试..." -ForegroundColor Green
        
        New-Item -ItemType Directory -Path "TestResults" -Force
        
        dotnet test ${{ env.TEST_PROJECT_NAME }}/${{ env.TEST_PROJECT_NAME }}.csproj `
          --configuration ${{ matrix.configuration }} `
          --framework ${{ env.DOTNET_FRAMEWORK_VERSION }} `
          --no-build `
          --verbosity normal `
          --logger "trx;LogFileName=test-results.trx" `
          --results-directory TestResults `
          --collect:"XPlat Code Coverage" `
          -- DataCollectionRunSettings.DataCollectors.DataCollector.Configuration.Format=cobertura

    - name: 代码覆盖率分析
      shell: pwsh
      run: |
        Write-Host "分析代码覆盖率..." -ForegroundColor Green
        
        $coverageFiles = Get-ChildItem -Path "TestResults" -Filter "*.cobertura.xml" -Recurse
        if ($coverageFiles) {
          Write-Host "找到覆盖率文件: $($coverageFiles.Count)" -ForegroundColor Yellow
          foreach ($file in $coverageFiles) {
            Write-Host "处理文件: $($file.FullName)" -ForegroundColor Cyan
            
            $coverageContent = Get-Content $file.FullName -Raw
            if ($coverageContent -match 'line-rate="([0-9.]+)"') {
              $coveragePercent = [math]::Round([double]$Matches[1] * 100, 2)
              Write-Host "代码覆盖率: $coveragePercent%" -ForegroundColor Green
              
              if ($coveragePercent -lt ${{ env.COVERAGE_THRESHOLD }}) {
                Write-Host "警告: 代码覆盖率低于阈值 ${{ env.COVERAGE_THRESHOLD }}%" -ForegroundColor Red
              }
            }
          }
        } else {
          Write-Host "警告: 未找到覆盖率文件" -ForegroundColor Yellow
        }

    - name: 运行集成测试
      shell: pwsh
      run: |
        Write-Host "开始集成测试..." -ForegroundColor Green
        
        $env:ZHONGCAD_SIMULATION = "true"
        $env:INTEGRATION_TEST_MODE = "true"
        $env:ZWCAD_API_MODE = "true"
        
        dotnet test ${{ env.TEST_PROJECT_NAME }}/${{ env.TEST_PROJECT_NAME }}.csproj `
          --configuration ${{ matrix.configuration }} `
          --framework ${{ env.DOTNET_FRAMEWORK_VERSION }} `
          --no-build `
          --verbosity detailed `
          --filter "Category=Integration" `
          --logger "trx;LogFileName=integration-results.trx" `
          --results-directory TestResults

    - name: 运行性能测试
      shell: pwsh
      continue-on-error: true
      run: |
        Write-Host "开始性能测试..." -ForegroundColor Green
        
        $env:PERFORMANCE_TEST_MODE = "true"
        $env:BENCHMARK_CONFIG_PATH = "${{ env.TEST_PROJECT_NAME }}/Performance/BenchmarkConfig.json"
        
        dotnet test ${{ env.TEST_PROJECT_NAME }}/${{ env.TEST_PROJECT_NAME }}.csproj `
          --configuration ${{ matrix.configuration }} `
          --framework ${{ env.DOTNET_FRAMEWORK_VERSION }} `
          --no-build `
          --verbosity normal `
          --filter "Category=Performance" `
          --logger "trx;LogFileName=performance-results.trx" `
          --results-directory TestResults

    # 修复：更新到 v4
    - name: 上传测试结果
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: test-results-windows-${{ matrix.configuration }}
        path: |
          TestResults/
        retention-days: 30

    - name: 发布测试结果
      uses: dorny/test-reporter@v1
      if: always()
      with:
        name: 测试报告 - Windows ${{ matrix.configuration }}
        path: TestResults/*.trx
        reporter: dotnet-trx

  # UI测试
  test-ui:
    name: UI自动化测试
    runs-on: windows-latest
    needs: test-windows
    
    steps:
    - name: 检出代码
      uses: actions/checkout@v4

    - name: 设置 .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}

    - name: 模拟中望CAD环境
      shell: pwsh
      run: |
        Write-Host "设置中望CAD模拟环境..." -ForegroundColor Yellow
        $env:ZHONGCAD_VERSION = "2024"
        $env:ZHONGCAD_PATH = "C:\Program Files\ZWSOFT\ZWCAD 2024"
        $env:ZWCAD_API_MODE = "true"
        
        New-Item -ItemType Directory -Path "ZWCAD_Mock" -Force
        Write-Host "中望CAD模拟环境已设置" -ForegroundColor Green

    - name: 还原NuGet包
      shell: pwsh
      run: |
        dotnet restore ${{ env.SOLUTION_NAME }}
        dotnet restore ${{ env.TEST_PROJECT_NAME }}/${{ env.TEST_PROJECT_NAME }}.csproj

    - name: 构建项目
      shell: pwsh
      run: |
        msbuild ${{ env.SOLUTION_NAME }} /p:Configuration=Release /p:Platform="Any CPU" /v:minimal

    - name: 运行UI测试 (模拟模式)
      shell: pwsh
      continue-on-error: true
      run: |
        Write-Host "开始UI自动化测试..." -ForegroundColor Green
        
        New-Item -ItemType Directory -Path "UITestResults" -Force
        
        $env:UI_TEST_MODE = "simulation"
        $env:ZWCAD_AUTOMATION = "true"
        $env:HEADLESS_MODE = "true"
        $env:ZHONGCAD_API_MODE = "true"
        
        Write-Host "UI测试环境变量已设置" -ForegroundColor Yellow
        
        dotnet test ${{ env.TEST_PROJECT_NAME }}/${{ env.TEST_PROJECT_NAME }}.csproj `
          --configuration Release `
          --framework ${{ env.DOTNET_FRAMEWORK_VERSION }} `
          --no-build `
          --verbosity normal `
          --filter "Category=UI" `
          --logger "trx;LogFileName=ui-results.trx" `
          --results-directory UITestResults
        
        Write-Host "UI测试完成" -ForegroundColor Green

    - name: 收集UI测试截图
      shell: pwsh
      if: always()
      run: |
        Write-Host "收集UI测试截图..." -ForegroundColor Green
        
        New-Item -ItemType Directory -Path "UI/TestScreenshots" -Force
        
        if (-not (Test-Path "UI/TestScreenshots/*.png")) {
          Write-Host "未找到截图，创建占位符文件" -ForegroundColor Yellow
          $placeholder = "没有UI测试截图生成"
          $placeholder | Out-File -FilePath "UI/TestScreenshots/placeholder.txt" -Encoding UTF8
        }

    # 修复：更新到 v4
    - name: 上传UI测试结果
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: ui-test-results
        path: |
          UITestResults/
          UI/TestScreenshots/
        retention-days: 30

  # 安全扫描
  security-scan:
    name: 安全扫描
    runs-on: ubuntu-latest
    needs: code-quality
    
    steps:
    - name: 检出代码
      uses: actions/checkout@v4

    - name: 设置 .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}

    - name: 运行安全扫描
      run: |
        dotnet restore ${{ env.SOLUTION_NAME }}
        echo "=== 检查易受攻击的包 ==="
        dotnet list package --vulnerable || true
        echo "=== 检查已弃用的包 ==="
        dotnet list package --deprecated || true
        echo "=== 检查过时的包 ==="
        dotnet list package --outdated || true

    - name: 运行CodeQL分析
      uses: github/codeql-action/init@v3
      with:
        languages: csharp

    - name: 自主构建
      run: |
        dotnet build ${{ env.SOLUTION_NAME }} --no-restore

    - name: 执行CodeQL分析
      uses: github/codeql-action/analyze@v3

  # 许可证检查
  license-check:
    name: 许可证检查
    runs-on: ubuntu-latest
    
    steps:
    - name: 检出代码
      uses: actions/checkout@v4

    - name: 设置 .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}

    - name: 许可证扫描
      run: |
        dotnet restore ${{ env.SOLUTION_NAME }}
        
        echo "=== 项目依赖包 ===" | tee -a licenses.txt
        dotnet list package --include-transitive | tee -a licenses.txt
        
        echo "=== 许可证合规性检查 ===" | tee -a licenses.txt
        echo "1. MIT许可证: 允许" | tee -a licenses.txt
        echo "2. Apache 2.0许可证: 允许" | tee -a licenses.txt
        echo "3. BSD许可证: 允许" | tee -a licenses.txt
        echo "4. GPL许可证: 需要审查" | tee -a licenses.txt
        
        echo "许可证扫描完成" >> $GITHUB_STEP_SUMMARY

  # 发布准备
  prepare-release:
    name: 发布准备
    runs-on: windows-latest
    needs: [test-windows, test-ui, security-scan, license-check]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
    steps:
    - name: 检出代码
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: 设置 .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}

    - name: 生成版本号
      id: version
      shell: pwsh
      run: |
        $version = "v1.0.0-${{ github.run_number }}"
        Write-Output "version=$version" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append
        Write-Host "Generated version: $version"

    - name: 构建发布版本
      shell: pwsh
      run: |
        dotnet restore ${{ env.SOLUTION_NAME }}
        dotnet build ${{ env.SOLUTION_NAME }} --configuration Release --no-restore

    - name: 运行发布测试
      shell: pwsh
      run: |
        dotnet test ${{ env.TEST_PROJECT_NAME }}/${{ env.TEST_PROJECT_NAME }}.csproj --configuration Release --no-build --filter "Category=UnitTest"

    - name: 打包发布文件
      shell: pwsh
      run: |
        # 创建发布目录
        New-Item -ItemType Directory -Path "release" -Force
        
        # 复制构建输出
        if (Test-Path "bin\Release\net48") {
          Copy-Item -Path "bin\Release\net48\*" -Destination "release\" -Force -Recurse
        } else {
          Write-Host "警告: 构建输出目录不存在" -ForegroundColor Yellow
        }
        
        # 复制配置文件
        if (Test-Path "docs") {
          New-Item -ItemType Directory -Path "release\docs" -Force
          Copy-Item -Path "docs\*.md" -Destination "release\docs\" -Force -ErrorAction SilentlyContinue
        }
        
        # 复制测试报告（如果存在）
        if (Test-Path "TestResults") {
          Copy-Item -Path "TestResults" -Destination "release\test-results" -Recurse -Force -ErrorAction SilentlyContinue
        }
        
        if (Test-Path "UITestResults") {
          Copy-Item -Path "UITestResults" -Destination "release\ui-test-results" -Recurse -Force -ErrorAction SilentlyContinue
        }
        
        # 打包
        $version = "${{ steps.version.outputs.version }}"
        Compress-Archive -Path "release\*" -DestinationPath "ZWDynLookup-$version.zip" -Force

    # 修复：更新到 v4
    - name: 上传发布包
      uses: actions/upload-artifact@v4
      with:
        name: release-package-${{ steps.version.outputs.version }}
        path: ZWDynLookup-${{ steps.version.outputs.version }}.zip
        retention-days: 90

    - name: 创建GitHub发布
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ steps.version.outputs.version }}
        name: Release ${{ steps.version.outputs.version }}
        body: |
          ## 🎯 中望CAD动态块查寻插件 ${{ steps.version.outputs.version }}
          
          ### 📋 更新内容
          - ✅ 完整的单元测试覆盖
          - ✅ 集成测试验证
          - ✅ 性能基准测试
          - ✅ UI自动化测试
          - ✅ 安全扫描通过
          - ✅ 许可证检查完成
          
          ### 📦 包含内容
          - ZWDynLookup.dll - 插件主程序
          - ZWDynLookup.pdb - 调试符号文件
          - docs/ - 完整文档
          - test-results/ - 测试报告
          
          ### 🔧 安装说明
          1. 将 ZWDynLookup.dll 复制到中望CAD插件目录
          2. 在中望CAD中加载插件
          3. 使用 BPARAMETER K 和 BACTIONTOOL L 命令
          
          ### 📞 技术支持
          如有问题请查看 docs/ 目录下的文档。
        files: |
          ZWDynLookup-${{ steps.version.outputs.version }}.zip
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # 通知和报告
  notify-results:
    name: 通知测试结果
    runs-on: ubuntu-latest
    needs: [test-windows, test-ui, security-scan, license-check]
    if: always()
    
    steps:
    # 修复：更新到 v4
    - name: 获取测试结果
      uses: actions/download-artifact@v4
      
    - name: 生成测试报告摘要
      run: |
        echo "## 📊 中望CAD动态块查寻插件 - 测试报告" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### 🏷️ 测试环境信息" >> $GITHUB_STEP_SUMMARY
        echo "- **操作系统**: 多平台" >> $GITHUB_STEP_SUMMARY
        echo "- **.NET版本**: ${{ env.DOTNET_VERSION }}" >> $GITHUB_STEP_SUMMARY
        echo "- **框架版本**: ${{ env.DOTNET_FRAMEWORK_VERSION }}" >> $GITHUB_STEP_SUMMARY
        echo "- **触发分支**: ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
        echo "- **触发事件**: ${{ github.event_name }}" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        
        echo "### ✅ 测试任务状态" >> $GITHUB_STEP_SUMMARY
        echo "| 测试类型 | 状态 | 说明 |" >> $GITHUB_STEP_SUMMARY
        echo "|---------|------|------|" >> $GITHUB_STEP_SUMMARY
        echo "| 代码质量检查 | ${{ needs.code-quality.result }} | SonarCloud分析 |" >> $GITHUB_STEP_SUMMARY
        echo "| Windows环境测试 | ${{ needs.test-windows.result }} | 单元/集成/性能测试 |" >> $GITHUB_STEP_SUMMARY
        echo "| UI自动化测试 | ${{ needs.test-ui.result }} | 用户界面测试 |" >> $GITHUB_STEP_SUMMARY
        echo "| 安全扫描 | ${{ needs.security-scan.result }} | 漏洞和依赖检查 |" >> $GITHUB_STEP_SUMMARY
        echo "| 许可证检查 | ${{ needs.license-check.result }} | 第三方库许可证 |" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        
        # 总体状态
        if [[ "${{ needs.code-quality.result }}" == "success" && "${{ needs.test-windows.result }}" == "success" && "${{ needs.security-scan.result }}" == "success" ]]; then
          echo "### 🎉 测试结果: ✅ 通过" >> $GITHUB_STEP_SUMMARY
          echo "所有核心测试均已通过，代码质量良好！" >> $GITHUB_STEP_SUMMARY
        else
          echo "### ⚠️ 测试结果: ❌ 失败" >> $GITHUB_STEP_SUMMARY
          echo "部分测试未通过，请查看详细信息并修复问题。" >> $GITHUB_STEP_SUMMARY
        fi
        
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### 📈 后续步骤" >> $GITHUB_STEP_SUMMARY
        if [[ "${{ github.ref_name }}" == "main" && "${{ needs.prepare-release.result }}" == "success" ]]; then
          echo "- ✅ 发布版本已创建" >> $GITHUB_STEP_SUMMARY
          echo "- 📦 发布包可在 Artifacts 中下载" >> $GITHUB_STEP_SUMMARY
        elif [[ "${{ github.ref_name }}" == "main" && "${{ needs.prepare-release.result }}" != "success" ]]; then
          echo "- ⚠️ 发布准备失败，请检查日志" >> $GITHUB_STEP_SUMMARY
        else
          echo "- 🔄 非 main 分支，跳过发布" >> $GITHUB_STEP_SUMMARY
        fi

    - name: 发送通知
      if: always()
      run: |
        if [[ "${{ needs.code-quality.result }}" == "success" && "${{ needs.test-windows.result }}" == "success" && "${{ needs.security-scan.result }}" == "success" ]]; then
          echo "🎉 测试通过！"
          echo "所有测试任务均已成功完成。"
        else
          echo "⚠️ 测试失败！"
          echo "请检查失败的测试任务并修复相关问题。"
        fi
