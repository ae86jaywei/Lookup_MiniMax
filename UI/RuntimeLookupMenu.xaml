<Window x:Class="ZWDynLookup.UI.RuntimeLookupMenu"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="查寻菜单" 
        Height="300" 
        Width="200"
        WindowStyle="None" 
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize">
    
    <Window.Resources>
        <Style x:Key="MenuItemStyle" TargetType="MenuItem">
            <Setter Property="Background" Value="#FF2D2D2D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FF3C3C3C"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF404040"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Foreground" Value="#FF666666"/>
                    <Setter Property="Cursor" Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="HeaderMenuItemStyle" TargetType="MenuItem">
            <Setter Property="Background" Value="#FF1E1E1E"/>
            <Setter Property="Foreground" Value="#FFCCCCCC"/>
            <Setter Property="BorderBrush" Value="#FF3C3C3C"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Arrow"/>
        </Style>
        
        <Style x:Key="SeparatorStyle" TargetType="Separator">
            <Setter Property="Background" Value="#FF3C3C3C"/>
            <Setter Property="Height" Value="1"/>
            <Setter Property="Margin" Value="5,2"/>
        </Style>
    </Window.Resources>

    <Border Background="#FF2D2D2D" 
            BorderBrush="#FF3C3C3C" 
            BorderThickness="1" 
            CornerRadius="3">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 菜单标题 -->
            <Border Grid.Row="0" 
                    Background="#FF1E1E1E" 
                    CornerRadius="3,3,0,0"
                    Padding="10,8">
                <TextBlock Text="查寻动作选择" 
                           Foreground="#FFCCCCCC" 
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
            </Border>

            <!-- 菜单项列表 -->
            <ScrollViewer Grid.Row="1" 
                          VerticalScrollBarVisibility="Auto" 
                          HorizontalScrollBarVisibility="Disabled">
                <Menu x:Name="MainMenu" 
                      Background="Transparent" 
                      BorderThickness="0"
                      ItemContainerStyle="{StaticResource MenuItemStyle}">
                    
                    <!-- 动态生成的菜单项将在这里 -->
                    <MenuItem Header="切换到动作1" 
                              Click="ActionMenuItem_Click"
                              Tag="Action1"/>
                    <MenuItem Header="切换到动作2" 
                              Click="ActionMenuItem_Click"
                              Tag="Action2"/>
                    <MenuItem Header="切换到动作3" 
                              Click="ActionMenuItem_Click"
                              Tag="Action3"/>
                    
                    <Separator Style="{StaticResource SeparatorStyle}"/>
                    
                    <MenuItem Header="查寻参数..." 
                              Click="LookupParameterMenuItem_Click"
                              Tag="LookupParameter"/>
                    <MenuItem Header="显示查寻表" 
                              Click="ShowLookupTableMenuItem_Click"
                              Tag="ShowLookupTable"/>
                    
                    <Separator Style="{StaticResource SeparatorStyle}"/>
                    
                    <MenuItem Header="属性" 
                              Click="PropertiesMenuItem_Click"
                              Tag="Properties"/>
                    
                </Menu>
            </ScrollViewer>

            <!-- 状态栏 -->
            <Border Grid.Row="2" 
                    Background="#FF1E1E1E" 
                    CornerRadius="0,0,3,3"
                    Padding="10,5">
                <TextBlock x:Name="StatusTextBlock" 
                           Text="选择动作" 
                           Foreground="#FF999999" 
                           FontSize="10"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
            </Border>
        </Grid>
    </Border>
</Window>